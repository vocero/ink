pub struct Story {}

const INK_VERSION_CURRENT: i32 = 21;
const INK_VERSION_MINIMUM_COMPATIBLE: i32 = 18;

impl Story {
    /// Construct a Story object using a JSON string compiled through inklecate.
    pub fn new_from_json(json: String) -> Self { Story {} }
}
